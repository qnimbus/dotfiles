README.md

# 1Password CLI
.config/op/
.config/op/**

# Claude (default: ignore all runtime state)
.claude/**
!.claude/
!.claude/skills/**
!.claude/settings.json
!.claude/plugins/installed_plugins.json
!.claude/plugins/known_marketplaces.json

{{ if ne .chezmoi.os "linux" }}
*.sh
**/*/*.sh
.chezmoiscripts/linux/**
{{- end }}

{{ if and (ne .chezmoi.os "linux") (ne .chezmoi.os "windows") }}
# .bash/ is excluded everywhere except Linux and Windows. On Windows, Git Bash
# reads ~/.bashrc (a symlink → ~/.bash/bashrc), so the directory must be
# deployed. On macOS and other platforms there is no Git Bash, so skip it.
.bash
{{- end }}

{{ if ne .chezmoi.os "windows" }}
AppData
Documents
Documents/*

.chezmoiscripts/windows/**
{{- end }}
